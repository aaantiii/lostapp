import { lazy } from 'react'

export const LazyAlertDialog = lazy(() => import('@components/AlertDialog'))
export const LazyDialog = lazy(() => import('@components/Dialog'))
export const LazyForm = lazy(() => import('@components/Form'))
